pipeline {
  agent any
  stages {
    stage('Source') { 
      steps {
			// Some Step
      }
    }
    stage('Compile') { 
      tools {
        // Specify Tool Name from your global tool configuration
		jdk 'jdk8'
        maven 'Maven'
      }
      steps {
			// Some Step
      }
    }
  }
}
